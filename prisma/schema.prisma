
generator client {
  provider = "prisma-client"
  output   = "./generated"
  moduleFormat = "cjs"
}

datasource db {
  provider = "postgresql"
}

model Actor {
  id String @id @default(uuid()) @db.Uuid
  author String
  createdAt String
  updatedAt String
}